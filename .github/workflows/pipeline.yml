
Entendi, você deseja combinar os três arquivos YAML em um único arquivo main.yml. Para fazer isso, você pode simplesmente copiar o conteúdo dos arquivos aws_tf_plan.yml e aws_tf_appy.yml e colá-los no arquivo main.yml. Aqui está como você pode fazer isso:

yaml
Copy code
name: "pipeline-rds"
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Plan:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Validate
        id: validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: terraform plan -input=false \
            -var "db_password=$DB_PASSWORD" -var "db_password=senha_segura" \
            -out=tfplan
        continue-on-error: true
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

  Apply:
    needs: Plan
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Validate
        id: validate
        run: terraform validate

      - name: Terraform Apply
        run: |
          terraform apply -auto-approve \
            -var "db_password=$DB_PASSWORD"
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}